# Normalize line endings so shell scripts survive a Windows checkout
# (CRLF in a .sh breaks bash in WSL / the devcontainer). Aligns with .editorconfig (LF).
* text=auto eol=lf

# Binary artifacts — never normalize.
*.png  binary
*.jpg  binary
*.pdf  binary
*.zst  binary
*.idx  binary
